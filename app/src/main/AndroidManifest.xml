<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.empty_try">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.Empty_try">
        <activity android:name=".huilv"  android:exported="true"></activity>
        <activity android:name=".hello"  android:exported="true"></activity>
        <activity android:name=".score"  android:exported="true"></activity>
        <activity android:name=".two_scorer1"  android:exported="true"></activity>
        <activity android:name=".two_scorers"  android:exported="true"></activity>

    </application>

</manifest>